# CHG-20260918-006 — A04 WebRTC-Clients

```yaml
source: a04-frontend
agent_id: 687f8f7b-27ef-4e23-a1fd-414258012112
status: FREIGEGEBEN (Recherche)
```

Steal: lokal sofort kappen; WebRTC = `response.cancel` + `output_audio_buffer.clear` (kein Truncate); Unlock erst nach cancelled/cleared; Call-Attempt-ID; Mic-Stop nur Exit; keine zweite VAD.

HEAD-Lücke: `suppressPlayback` fällt bei `response.created`. Hartregeln: Track-Gate nur mit Clear; `track.stop()` nicht während Barge-in.
