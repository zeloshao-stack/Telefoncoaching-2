# CHG-20260919-014 — A04 sichtbare Szenenziele (K04)

```yaml
change_id: CHG-20260919-014
status: FREIGEGEBEN
primary_owner: A04
reviewer_k: K04
code: yes
gate: Gate A / G2
```

**BO:** Szenenkarten tragen den Auftrag sichtbar. Drei Ziele nicht hinter „Was zählt“.

## A

`/`: Titel, Badge Telefonat, drei Ziele (Fetttitel + `beat.why` aus `scene-intel`), Initialen + Chip, „Gespräch starten“. Kein Klapp, keine Tabs, kein Foto, kein Paywall.

Ohne Intel: vorhandene Titel + ein `practiceFocus` (`focusLine`). Kein synthetisiertes Why.

`/szenario/[id]`: Ziel, Lage, dieselben drei Ziele mit Why, Hinweis, Start.

`catalogSurface` / `trainingPageSurface` tragen `goals`. Session-POST unverändert (`scenarioId` + `focusId`). Freeze: `liveCall`, `bargeIn`, `openai-realtime`.

## K04

```text
GEGENPRÜFUNG
Prüfumfang: / Kartenstapel, /szenario/S01, Why=Intel, Isolation evalBeats/Player, Freeze Transport, XSS-Textknoten, Paywall/Foto/Tabs/7er-Liste.
Durchgeführte Angriffe:
- Browser 127.0.0.1:43147 / — CDP: 3 Karten, 9 Ziele, 9 Why, 3× Telefonat, EL/FB/AH, 3× Start; Was zählt=false; Tabs=0; img=0; Kein Drängen=false; Qualify-Zeile=false.
- Browser /szenario/S01 — Ziel + drei Why + Hinweis + Start, kein Klapp.
- Unit scene-card / scene-briefing / scene-intel 14/14.
- Grep: liveCall, bargeIn, openai-realtime ohne scene-intel/SceneGoals.

BEFUNDE
- K04-1 MicGate auf /szenario vorbestehend. NOTE.
- K04-2 Safari nicht in diesem Lauf. NOTE.
- K04-3 countTitles bleibt am Modell, nicht auf der Fläche. NOTE.

URTEIL
FREIGEGEBEN
```

```text
AUFTRAG_ID: CHG-20260919-014
ERGEBNIS: Katalog- und Briefing-Karten zeigen drei Trainingsziele ohne Extra-Klick.
GEÄNDERTE_DATEIEN_UND_VERTRÄGE: SceneCard.tsx; SceneGoals.tsx (neu); app/szenario/[id]/page.tsx; lib/scene-card.ts (VisibleGoal, catalogSurface); lib/scene-briefing.ts (trainingPageSurface.goals); lib/scene-intel.ts (Public beats+why, Kommentar); Tests. Session-POST unverändert. Kein Role-Player-/Transport-Vertrag.
NACHWEISE_UND_TESTS: 14 Unit grün. Browser /: Ziele lesbar ohne Klick. /szenario/S01: Why sichtbar.
QUALITÄTSWIRKUNG: PLAUSIBEL Auftrag auf der Karte lesbar. Kein 2×-Claim.
KOSTENWIRKUNG: keine Provider.
RISIKEN_UND_OFFENE_HYPOTHESEN: Why jetzt Nutzer-UI (BO), nicht nur Coach. A08 neue Fälle S04–S08 noch nicht.
GEGENPRÜFER_URTEIL: FREIGEGEBEN
NÄCHSTE_ROLLE: A00
KONTEXT_FÜR_NÄCHSTEN_CHAT: UI rendert beat.why. Fallback = Titel + practiceFocus. evalBeats/qualify bleiben weg. Transport-Freeze halten. Neue Karten = A08 Inhalt.
```
