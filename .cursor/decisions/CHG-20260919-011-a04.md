# CHG-20260919-011 — A04 Frontend-Ebene (K04)

```yaml
change_id: CHG-20260919-011
source: a04-frontend
reviewer: k04
status: FREIGEGEBEN (Fachentwurf; kein Produktcode)
gate: Gate A / G2
primary_owner: A04
reviewer_k: K04
code: none
bo_a00:
  live: keine Scorecard/HUD
  nach_hangup: Gong-Player-Logik (Momente, Anker, eine Note)
  board: Session-Pipeline, kein Forecast-Revenue
non_goals:
  - Produktcode in diesem Chat
  - GO-2/GO-T Barge-in zerlegen oder mitschreiben
  - Live-HUD, Waveform, Moments, Scorecard während der Leitung
  - Rubrik in den Role Player
  - Avatar im Kern
  - Billing, Mandant, Studio, Forecast-Theater
  - Fake-Timestamps auf der Debrief-Timeline
  - 1:1 Gong/Hyperbound/Careertrainer-CSS
```

Gelesen, kein Vollscan: Change Card `CHG-20260919-011.md`; Steal-Dossiers `010-gong`, `010-hyperbound`, `010-careertrainer`; `app/page.tsx`; `components/training/ConversationView.tsx`; `components/training/EvaluationView.tsx`; `app/verlauf/page.tsx`; `lib/session-types.ts`; `src/role-engine/types.ts` (`TranscriptTurn` ohne Zeit); `lib/live-status.ts` + `lib/__tests__/live-status.test.ts`; `CHG-20260919-008-entscheidungspaket.md` (GO-2 / Fork U / GO-T); `CHG-20260919-007-a04-webrtc.md` (H5 / ConversationView = GO-2); `CHG-20260919-009-a06.md` (GO-T, ConversationView unberührt).

---

## Phase 1 — A Auftragsverständnis

```text
AUFTRAGSVERSTÄNDNIS
- gewünschte Nutzerwirkung: Die App fühlt sich eine Ebene höher an. / ist eine Handlung (diesen Anruf starten), nicht ein Katalog-Wald. Die Leitung bleibt Telefon, nicht Chat+Score. Nach Auflegen liegt ein Debrief-Player mit Momenten auf einer Timeline (Gong-Steal). Verlauf ist ein Board aus Karten und Status, keine flache Liste.
- betroffene Komponente: Home, Live-Chrome (nur Präsentation), Auswertung, Verlauf. Nicht Transport, nicht Evaluator, nicht Figur.
- Nicht Ziele: Code hier; Barge-in/GO-2/GO-T anfassen; Live-HUD; Waveform im Call; Forecast-Umsatz; Avatar; Rubrik im Player; Fake-ms auf Turns.
- Annahmen: A00-011 hebt die 010-Gong-Sperre „Homepage kein Gate-A-GO“ und „Deal-Board Avoid“ für die Übungs-Pipeline auf. Fork U (Chat+Orb) bleibt GO-2-nah — Call-Chrome nur als Hülle um die bestehende FSM. TranscriptTurn hat kein at/ms (VERIFIZIERT) → Timeline ordinal.
- Unbekannt: GO-2-Schluss weiter UNBEKANNT. Ob ein Live-Mitschnitt existiert, den ein Player abspielen könnte: nein im DTO (VERIFIZIERT: Turns = id/speaker/text).
- Akzeptanz dieser Datei: 6 Patch-Skizzen mit Dateipfaden; Freeze-Liste GO-2/GO-T; A dann K; kein Produktcode.
```

---

## Phase 2 — A Impact

```text
IMPACT_PLAN
- Dateien jetzt: nur .cursor/decisions/CHG-20260919-011-a04.md
- Dateien später (Bau, nicht dieser Chat): siehe sechs Skizzen.
- Verträge: SessionDTO.status bleibt active|ended. Evaluation unverändert. TranscriptTurn unverändert (kein at erfinden). callStatusLine unverändert.
- abhängige Rollen: A03 Copy der drei Momente und Spaltennamen (Neben, nur Wortlaut). A05 nur falls später turn.at — nicht in diesen Skizzen. A06/A14 nicht. A08 Inhalt der 1+1-Note nicht neu.
- Wiederverwendung: StartSessionButton; callStatusLine / VoiceOrb / MicPrompt / endCall / persistLiveTurn; EvaluationView Stärke + Schlüsselmoment + Repeat; listRecentSessions; analyzeCall / skillHistory als Board-Rail, nicht als Spalte.
- kleinster sicherer Umfang: drei getrennte Bau-GOs (unten). Nie Home+Chrome+Player+Board in einem Diff mit liveCall.
- Zieltests später: debrief-moments, session-board, bestehende live-status-Tests unverändert grün. Kein neuer Barge-in-Test der Transport zerlegt.
- Qualität: weniger Cognitive Load auf / und Live; Lernen wandert auf Debrief. Unbelegt bis Browser.
- Kosten: keine Provider, keine Deps, keine Extra-Modellcalls.
```

### Freeze GO-2 / GO-T (verbindlich)

Nicht anfassen, nicht „nebenbei“ refaktorieren, nicht in denselben Commit wie UI-Hülle:

| Datei / Symbol | Warum |
|---|---|
| `components/training/liveCall.ts` | GO-T Transport, Stufe A/B, Playback |
| `components/training/bargeIn.ts` | Kaskaden-VAD; Live-Pfad nicht zweite VAD |
| `lib/live-text.ts` | Cut, Dedupe, Heard/Unlock |
| `lib/openai-realtime.ts` | Provider-Events, Cancel/Clear |
| `ConversationView` Hooks/Effects | `useLiveCall`, `startBargeInWatch`, `persistLiveTurn`, `endCall`, `noteInterrupt`, `HANGUP_BEAT_MS`, `INTERRUPT_FLASH_MS`, Mic `stopStream` / `pagehide` |
| `lib/__tests__/openai-realtime.test.ts` | Transport-Nachweis, nicht umbauen |

Erlaubt an `ConversationView.tsx`: **nur der `return (`-JSX-Baum**, und nur indem er eine neue Präsentationskomponente füttert. Kein neuer `useState` für Score/Moments/Waveform. Kein zweites Audio-Element neben dem bestehenden hidden `<audio>`.

Tipp-Modus (`typedOnly`) behält die Chatwand — Vertrag in `lib/live-status.ts` Zeile 1.

---

## Phase 3 — A Fachentwurf

### Ist-Stand (Messlatte, VERIFIZIERT 2026-09-19)

| Fläche | Heute | Gap zur 011-Wirkung |
|---|---|---|
| `/` `app/page.tsx` | Hero ohne CTA; Featured + `StartSessionButton`; Rest-Liste und „Ihre Fälle“ je eigene Start-Buttons | Mehrere gleichwertige Handlungen |
| Live `ConversationView.tsx` | Name, Fokus-Satz, LIVE-Essay, Briefing-Karte, Chatwand, Orb + `callStatusLine`, Leitung/Hören/Auflegen | Chat+Dossier, nicht Telefon-Chrome. FSM und Barge-in sitzen hier — das bleibt |
| Debrief `EvaluationView.tsx` | Gestapelte Karten: Stärke, Schlüsselmoment, Next, Rubrik-`<details>`, Transkript-Liste mit Markern | Momente existieren, aber keine Timeline, keine Player-Nachbarschaft Audio↔Note |
| `/verlauf` | Flache `<ul>` der letzten 20; Skills-Zeilen; Muster-Karte | Keine Spalten, Status nur Badge auf der Karte |
| Turn | `{ id, speaker, text }` | Kein `at`, kein `heard_ms` im DTO → keine Wanduhr |

### Steal / Avoid (nur UI-Ebene)

| Quelle | Steal | Avoid |
|---|---|---|
| Gong E / 011 A00 | Player **nach** Hangup; Timeline + Marker; Scorecard **neben** der Stelle; Repeat = Snippet | Call-Page live; Waveform in der Leitung; Forecast-Umsatz; Gong-Lila |
| Hyperbound C/D | Eine Primärhandlung auf `/`; Featured zuerst; Live ohne Score | Practice-HUD, Filler live, Video-Chrome, Scorecard-Preview vor dem Wählen |
| Careertrainer C/D | Eine Handlung; Personen-Karte; eine Hörer-Zeile; Transkript optional | Vier Radios; Radar; 0–10-Galerie auf `/`; Face-to-face |

---

### Sechs Patch-Skizzen

Bau-Reihenfolge ist nicht die Listennummer. Siehe „GO-Schnitt“ nach P6.

#### P1 — Startseite: eine Handlung

**Dateien:** `app/page.tsx`  
**Nicht:** `StartSessionButton.tsx` (Focus-Picker und Prefetch bleiben), `AppSidebar.tsx`, Vertical-Radios (gibt es nicht — nicht einführen).

**Wirkung:** Hero und Featured werden eine Fläche. Ein Satz Nutzen, eine Gegenseite, ein `StartSessionButton`, ein Link „Briefing zuerst“. Sitzungszähler bleibt ehrlicher Proof (kein G2, keine Win-Rate).

**Rest und eigene Fälle:** keine zweiten Primär-Starts. Zeilen werden Wahlkarten: Titel, `callLabel`, Cold-Badge „Legt schnell auf“ bleibt sichtbar, Link auf `/szenario/[id]`. Start passiert im Briefing oder nach Rückkehr auf die eine Handlung.

**Wireframe:**

```text
[ Hero + Featured ]
  Nutzenzeile
  Name · Lage · Heute üben
  [ Gespräch starten ]   Briefing zuerst
  n Sitzungen | noch keine

[ Weitere Drills ]          ← sekundär, kein Primär-Button
  S02  Kalt  Legt schnell auf  → Briefing
  S03  …                       → Briefing

[ Ihre Fälle ]              ← gleich sekundär
```

**Test später:** Primär-CTA-Zählung auf `/`: genau ein `StartSessionButton` im Featured-Band. Bestehende Session-POST unverändert.

**Qualität / Risiko:** S02 braucht einen Klick mehr. Absicht. Cold-Härte darf auf der Wahlkarte nicht verschwinden.

---

#### P2 — Call-Chrome (Hülle, nicht FSM)

**Dateien:** `components/training/CallChrome.tsx` (neu, nur Props), `components/training/ConversationView.tsx` (**nur JSX-`return`**, Hooks unverändert).  
**Freeze:** Tabelle oben. `lib/live-status.ts` unverändert (bestehende Tests bleiben der Chrome-Vertrag).

**Wirkung:** Live sieht aus wie Leitung, nicht wie Chat-Dossier. Chrome rendert ausschließlich abgeleiteten Zustand, den ConversationView schon hat.

**Props (Skizze, kein Code):** `counterpartName`, `scenarioTitle`, `statusLine` (= `callStatusLine(...)`), `orbPhase`, `micStream`, `hangupBeat`, `ended`, `onHangup`, `audioBlocked` + `onUnlock`. Kein `score`, kein `moments`, kein `waveform`.

**Während `liveOnly && active` sichtbar:** Name, eine Statuszeile, Orb, Auflegen, Audio-Blocked-Banner, `practiceCue` (Repeat-Anker, kein Examiner).  
**Währenddessen nicht sichtbar:** Briefing-Karte, LIVE-Essay / `EngineBadge`, Chatwand (→ P2b im selben JSX-Pass).  
**Tipp-Modus:** heutiges Chat-Chrome bleibt.

**Leitung an / Hören an:** Funktionsknöpfe, kein HUD. In Overflow oder untergeordnet, nicht drei gleich große Primärbuttons neben Auflegen.

**GO-2-Regel:** Wer in diesem Patch `useEffect`, `useLiveCall`, `startBargeInWatch` oder Mic-Stop ändert, hat den Patch gesprengt. Checkliste im PR: Diff von ConversationView enthält keine dieser Symbole.

**Test später:** `lib/__tests__/live-status.test.ts` unverändert grün. Kein neuer Transport-Test.

---

#### P2b — Live-Transkript nicht als Chatwand

**Gehört in denselben ConversationView-JSX-Pass wie P2.** Kein zweiter Commit auf die GO-2-Datei.

**Dateien:** nur der JSX-Zweig in `ConversationView.tsx`, der `session.turns.map` rendert.

**Wirkung:** Im Live-Audio-Zweig keine Bubble-Wand. Optionaler Schalter „Mitschnitt zeigen“ (Careertrainer-Transkript-Button, `HYPOTHESE` 010-careertrainer H1). `persistLiveTurn` läuft weiter — UI blendet, speichert nicht weniger.

**Nicht:** Moments/POI live; Score live; Parallelzustand „heard moments“. `interruptFlash` bleibt in der Statuszeile.

**Tipp-Modus und `typedOnly`:** Chatwand bleibt.

**Test später:** kein Persist-Vertrag ändern. Wenn ein Component-Test kommt: `liveOnly` → Turn-Liste nicht im Baum, Statuszeile weiter `aria-live`.

---

#### P3 — Debrief-Momente (deterministisch, keine Wanduhr)

**Dateien:** `lib/debrief-moments.ts` (neu), `lib/__tests__/debrief-moments.test.ts` (neu).  
**Nicht:** `src/role-engine/types.ts`, Evaluator, `lib/evaluation.ts`, keine neue API.

**Wirkung:** Reine Ableitung aus vorhandenem DTO.

```text
Eingang: SessionDTO.turns + SessionDTO.evaluation
Ausgang:
  segments[]: { turnId, speaker, weight }   weight = Wortzahl, nicht ms
  markers[]:  { turnId, kind, label }
    kind = moment | strength | evidence
  selectedTurnId = keyMoment.turnId ?? importantMomentTurnId
System-Turns: kein Balken, optional Endhaken (Auflegen).
```

**VERIFIZIERT:** `TranscriptTurn` hat kein `at`. Marker-Labels aus schon gerenderten Worten (Schlüsselmoment, Stärke, Dimensionsname) — keine neuen Coach-Sätze.

**XSS:** Strings bleiben Strings. Renderer (P4) wie heute: `{turn.text}` / `„{quote}“`, kein HTML.

**Test:** drei Fixtures — leeres Gespräch; nur Figur; Stärke+Moment+zwei evidenceTurnIds. `weight`-Summe > 0 nur bei gesprochenen Turns. Kein Feld `ms`.

---

#### P4 — Debrief-Timeline (Gong-Nachbarschaft, kein Mitschnitt-Player)

**Dateien:** `components/training/DebriefTimeline.tsx` (neu).  
**Nicht:** `speechPlayer.ts` Autoplay, kein neues `<audio>` auf der Auswertung, kein Shared-`audioEl` mit der Live-Leitung.

**Wirkung:** Sprecherbalken (Sie vs. `counterpartName`), Playhead = ausgewählter Turn, Marker klickbar → Sprung zur Transkriptzeile (`id` am `<li>`). Eine Note bleibt die bestehende Fokus-Note, nicht acht Haken auf der Timeline.

**v1 ohne Wiedergabe der ganzen Leitung.** Es gibt keinen Call-Recording im DTO. „Player“ = Navigation + Anker, nicht Waveform. Per-Turn-TTS (`SpeakButton`) **defer** — sonst neuer Audio-Lifecycle auf `/auswertung` (Stop, Unlock, Safari). Repeat-Button bleibt in EvaluationView.

**Nicht Gong-Lila, nicht Screen-Share-Balken, nicht Filler-POI, nicht Library/Schere.**

**Wireframe:**

```text
+-- Debrief -----------------------------------+
| Timeline  [Sie====][Leitner==][Sie=] ●       |
|           ^Stärke        ^Moment              |
| Stelle + Zitat + eine Note /4                 |
| Transkript (Sprungziel, schon markiert)       |
+----------------------------------------------+
```

**Test:** Click-Handler setzt `selectedTurnId`; ohne Evaluation keine erfundenen Marker.

---

#### P5 — EvaluationView als Player-Schale

**Dateien:** `components/training/EvaluationView.tsx`.  
**Nicht:** `/api/sessions/.../evaluation`, Repeat-POST, Coach-POST.

**Wirkung:** Dieselbe 1+1-Note (Stärke, Schlüsselmoment, Nächstes Mal, Repeat) sitzt **neben** der Timeline, nicht als dritte Story unter einem zweiten Transkript. Rubrik und Vergleich bleiben `<details>`. Gesprächsbeobachtung bleibt eingeklappt, keine Note.

**Layout:** breiter als `max-w-3xl` (Player-Nachbarschaft). Mobil: Timeline oben, Note darunter (Gong-Help Mobile kehrt Desktop-Spalten um — Steal ist Nachbarschaft, nicht Spalte; 010-gong K02-1).

**Hangup-Kopf** (`endedHeading`, `hangupReasonLine`) bleibt. Keine Live-Route, kein Examiner-Einstieg von `/sitzung`.

**Test:** bestehende Repeat-/Coach-Pfade unverändert. Kein `dangerouslySetInnerHTML`.

---

#### P6 — Verlauf als Board

**Dateien:** `app/verlauf/page.tsx`, `components/training/SessionBoard.tsx` (neu), `lib/session-board.ts` (neu), `lib/__tests__/session-board.test.ts` (neu).  
**Nicht:** `listRecentSessions` Signatur, keine neue Status-Spalte in SQLite, kein Forecast, keine Euro, keine Win-Rate.

**Spalten — nur abgeleitet:**

| Spalte | Regel (bestehende Felder) |
|---|---|
| Offen | `status === "active"` |
| Nachbereiten | `status === "ended" && !evaluation` |
| Ausgewertet | `status === "ended" && evaluation` |

`mode === "repeat"` ist Badge auf der Karte, keine vierte Forecast-Spalte. `realCallId` bleibt Link-Kontext, keine Revenue-Spalte.

**Karte:** `scenarioTitle`, `counterpartName`, Zeitpunkt, Badges, bei Ende `nextStep` oder `summary` — wie heute, nur in Spalte. `href` unverändert: ended → `/auswertung`, sonst `/sitzung/[id]`.

**Rail oben:** „Was sich wiederholt“ + Skills bleiben eine Karte, keine Board-Spalte.

**Leer:** heutiger Honest-Empty (Honorarvergleich), nicht ein leeres Kanban-Theater.

**Test:** Fixture active / ended-ohne-Eval / ended-mit-Eval / repeat-active. Keine Spalte „gewonnen“.

---

### GO-Schnitt (nicht ein Diff)

010-gong verbot Home+Player+Karten in einem GO. 011 will die Ebene heben, nicht den Transport. Drei Bau-GOs, ein Thema pro Diff:

| GO | Patches | ConversationView | Risiko |
|---|---|---|---|
| **UI-A** Home + Board | P1 + P6 | nein | niedrig |
| **UI-B** Debrief-Player | P3 + P4 + P5 | nein | mittel (Layout, XSS-Disziplin) |
| **UI-C** Call-Chrome | P2 + P2b | JSX-only | hoch — GO-2-Nachbar; zuletzt, eigener PR, Freeze-Checkliste |

UI-A und UI-B dürfen parallel geplant werden. UI-C nicht mit GO-T/009 und nicht mit UI-B mischen.

---

### Qualitäts- / Kostenwirkung

- Qualität: `PLAUSIBEL` — weniger konkurrierende Starts, Live ohne Examiner, Debrief findet den Moment, Verlauf zeigt Pipeline-Status. Kein VERIFIZIERT-Lerntransfer.
- Kosten: keine Laufzeitkosten, keine neuen Modelle. Eine Entwicklungsfläche mehr (Timeline-Helfer + Board-Helfer), dafür kein Provider.

### Offene Hypothesen (A)

- **H1:** Transkript-Schalter senkt Last ohne weniger Repeat. Falsifikation: 5 Live-Calls mit/ohne sichtbare Turns, Repeat-Klick auf `/auswertung`.
- **H2:** Wortzahl-Balken werden als Zeit gelesen. Mitigierung: sichtbare Beschriftung „Reihenfolge der Züge, keine Uhr“. Falsifikation: 3 Nutzer, „wo war der Moment?“.
- **H3:** JSX-only-Diff an ConversationView bleibt nicht JSX-only. Mitigierung: UI-C Checkliste + K04-Diff-Lesung vor Merge.

---

## Phase 4 — K Gegenprüfung (unabhängig)

K04 hat Auftrag + A-Ergebnis, nicht die interne Begründung der Entwurfsrunde. Auftrag: Frontend-Ebene, 6 Skizzen, GO-2/GO-T Barge-in nicht zerlegen, Gong-Player nach Hangup nicht Live-HUD, Verlauf Board, kein Code.

```text
GEGENPRÜFUNG
Prüfumfang:
- Change Card 011 vs sechs Skizzen
- ConversationView / liveCall / bargeIn / live-text gegen Freeze
- EvaluationView + TranscriptTurn gegen Fake-Player
- verlauf/page.tsx gegen Forecast-Creep
- app/page.tsx gegen „eine Handlung“
- 007-H5 (jeder Chrome-Patch = GO-2-Kollision) vs 011 Call-Chrome
- 008 Fork U geparkt vs UI-C
- XSS, Mic-Leck, verspätete Events, paralleler UI-Zustand
- Chrome/Safari nur als Checkliste (kein Browserlauf, kein Code)

Durchgeführte Angriffe:
- Langsames/doppeltes/fremdes Event: ändert P2 Persist oder Generation? 
- Live-HUD-Einschleppung in P2/P4
- Timeline als erfundene Uhr
- Board als Deal-Forecast
- Zwei ConversationView-Patches
- Debrief-TTS als zweites Audio
- StartSessionButton-Anzahl nach P1
- Tests, die Barge-in zerlegen würden
```

### BEFUNDE

- **K04-1**  
  Fundstelle: P2 + P2b; `ConversationView.tsx`; 007-a04 H5 / K04 dort: „Jeder kleine Chrome-Patch ist GO-2-Kollision.“ 008: GO-2-Schluss UNBEKANNT, Fork U geparkt.  
  Schweregrad: **MAJOR**  
  Verletztes Kriterium: GO-2/GO-T nicht zerlegen; UI-Zustand abgeleitet; keine heimliche FSM.  
  Nachweis: A erlaubt JSX-`return`-Edit. Derselbe File enthält Barge-in-Effects (`startBargeInWatch` ~Z.246, `useLiveCall` ~Z.137, `endCall` ~Z.404). Ein „nur JSX“-Diff ist in Git nicht erzwingbar. Zwei Themen (Chrome + Transkript) verdoppeln die Chance, einen Hook mitzuziehen.  
  Korrektur: UI-C = ein PR. Checkliste verboten: `useLiveCall`, `startBargeInWatch`, `persistLiveTurn`, `endCall`, `noteInterrupt`, `stopStream`, `HANGUP_BEAT`, `INTERRUPT_FLASH`. CallChrome props-only, keine eigenen Effects. Nicht mit 009/GO-T mergen. In A so nachziehen.

- **K04-2**  
  Fundstelle: P4 „Player“; `TranscriptTurn` ohne `at`; EvaluationView hat keinen Mitschnitt.  
  Schweregrad: **MINOR**  
  Verletztes Kriterium: keine erfundene UI; Gong-Help = Recording-Player.  
  Nachweis: Help „move through a recording“. Unser DTO hat keine Aufnahme. A sagt v1 ohne Wiedergabe — gut. Wort „Player“ im Titel kann den nächsten Chat zu `<audio>` + Waveform verleiten.  
  Korrektur: In Übergabe und P4-Titel „Timeline + Sprung“, nicht „Call-Recording-Player“. Kein `SpeakButton`-Mitbau in v1 (A defer — halten).

- **K04-3**  
  Fundstelle: P1 Rest ohne `StartSessionButton`; `app/page.tsx` Rest-Schleife Z.181–188 hat heute je einen Start.  
  Schweregrad: **MINOR**  
  Verletztes Kriterium: eine Handlung vs. versteckte Härte.  
  Nachweis: S02-Cold-Badge sitzt an der Rest-Zeile. Wenn die Zeile nur noch „Briefing“ ist, bleibt Härte nur wenn Badge Pflicht ist. A sagt das — als Akzeptanzkriterium festziehen: Cold-Badge auf jeder Wahlkarte. Featured bleibt `catalog[0]`, nicht hart S01 (Vertical filtert).

- **K04-4**  
  Fundstelle: P6 drei Spalten; `listRecentSessions` liefert `status` + `evaluation`; Hangup navigiert nach `HANGUP_BEAT_MS` auf Auswertung, die Eval nachzieht.  
  Schweregrad: **NOTE**  
  Verletztes Kriterium: Board nicht leer-theatralisch.  
  Nachweis: „Nachbereiten“ ist der kurze Spalt zwischen `/end` und Eval-POST. Oft leer. Zulässig als ehrliche Spalte, nicht als Pipeline-KPI. Kein Nachzug.

- **K04-5**  
  Fundstelle: P2 „kein neuer useState für Score/Moments“. Transkript-Schalter braucht lokalen UI-State.  
  Schweregrad: **MINOR**  
  Verletztes Kriterium: UI-Zustand abgeleitet, nicht parallel dupliziert.  
  Nachweis: Ein `showTranscript: boolean` ist View-Preference, kein zweites Session-Modell — zulässig. Darf nicht Turns/Phase/Heard spiegeln. Default live = aus, typedOnly = an (abgeleitet).  
  Korrektur: In P2b: Preference nur, Default aus `liveOnly`/`typedOnly`.

- **K04-6**  
  Fundstelle: keine Component-Tests für ConversationView (a09-Batterie: „jsdom fehlt“).  
  Schweregrad: **NOTE**  
  Verletztes Kriterium: jede Verhaltensänderung braucht Test.  
  Nachweis: Testbarer Kern ist P3 + P6 (reine Ableitung). P1 CTA-Zahl und P4 Sprung brauchen später Playwright. Transport-Tests nicht anfassen. A trennt das korrekt.

- **K04-7**  
  Fundstelle: P5 breiteres Layout; AppShell Sidebar 64.  
  Schweregrad: **NOTE**  
  Verletztes Kriterium: Zugang / Mobile.  
  Nachweis: 010-gong E.1 Mobile kehrt Spalten um. A zitiert K02-1. Kein erfundener Desktop-Zwang. Kein Nachzug.

Keine erfundenen Blocker. Kein Barge-in-Patch in den Skizzen. Kein Live-HUD. Kein Forecast. Kein Rubrik-Leck. XSS-Pfad benannt. Mic-Lifecycle in Freeze.

```text
URTEIL
NACHARBEIT
```

Nacharbeit nur Dossier: K04-1 Checkliste + ein UI-C-PR; K04-2 Wortlaut Player; K04-3 Cold-Badge-Akzeptanz; K04-5 Preference-Regel. Kein Code.

---

## Phase 5 — A Nachbesserung

```text
BEFUND_ID: K04-1
AKZEPTIERT
BEGRÜNDUNG: ConversationView ist GO-2-Träger. JSX-only ist Absicht, nicht Mechanik.
ÄNDERUNG: UI-C = genau ein PR für P2+P2b. Verbotsliste der Symbole in Freeze und GO-Schnitt. CallChrome ohne Effects. Nicht mit GO-T mergen.
NACHWEIS: Abschnitte Freeze + GO-Schnitt + P2 nach diesem Befund.
RESTRISIKO: Menschlicher Diff-Fehler. K04 liest den UI-C-Diff vor Merge (H3).

BEFUND_ID: K04-2
AKZEPTIERT
BEGRÜNDUNG: Ohne Recording ist „Player“ eine Metapher.
ÄNDERUNG: P4-Titel und Übergabe: Timeline + Sprung. v1 ohne TTS, ohne Waveform, ohne zweites audio.
NACHWEIS: P4 und Phase 7.
RESTRISIKO: Jemand bringt später SpeakButton mit — eigene Karte, Audio-Stop-Nachweis.

BEFUND_ID: K04-3
AKZEPTIERT
BEGRÜNDUNG: Eine Handlung darf S02 nicht weichspülen.
ÄNDERUNG: P1-Akzeptanz: Cold-Badge Pflicht auf Wahlkarten; Featured = catalog[0] nach Vertical-Filter.
NACHWEIS: P1.
RESTRISIKO: keines für Gate A.

BEFUND_ID: K04-5
AKZEPTIERT
BEGRÜNDUNG: Schalter ist Preference, kein Shadow-Session-State.
ÄNDERUNG: P2b Default = !liveOnly || typedOnly. Kein Spiegel von phase/turns.
NACHWEIS: P2b.
RESTRISIKO: H1 unbelegt.

BEFUND_ID: K04-4, K04-6, K04-7
AKZEPTIERT als NOTE
BEGRÜNDUNG: keine Dossier-Änderung nötig.
ÄNDERUNG: keine.
NACHWEIS: —
RESTRISIKO: Nachbereiten-Spalte oft leer; Chrome-E2E fehlt bis Playwright.
```

---

## Phase 6 — K Retest

Nur K04-1, K04-2, K04-3, K04-5 und Nachbar (Freeze, kein HUD, kein Forecast).

- Freeze-Tabelle + UI-C = ein PR + Symbolverbot: **gehalten**.  
- P4 ohne Recording-Player-Anspruch, TTS defer: **gehalten**.  
- P1 Cold-Badge + eine Featured-Start: **gehalten**.  
- P2b Preference-Default: **gehalten**.  
- Kein nachträglicher liveCall-/bargeIn-Pfad: **gehalten**.

```text
URTEIL
FREIGEGEBEN
```

Fachentwurf. Kein Produktcode. Bau erst nach A00-Schnitt UI-A / UI-B / UI-C.

---

## Phase 7 — Übergabe

```text
AUFTRAG_ID: CHG-20260919-011
ERGEBNIS: A04-Fachentwurf Frontend-Ebene. Sechs Skizzen, drei Bau-GOs. Live bleibt ohne HUD. Debrief = Timeline+Sprung+bestehende 1+1-Note. Verlauf = Status-Board. GO-2/GO-T Barge-in eingefroren.
GEÄNDERTE_DATEIEN_UND_VERTRÄGE: .cursor/decisions/CHG-20260919-011-a04.md — keine Produktverträge, kein Code. SessionDTO/TranscriptTurn unverändert.
NACHWEISE_UND_TESTS: Lesend page.tsx, ConversationView, EvaluationView, verlauf, session-types, TranscriptTurn, live-status-Tests, 007/008/009/010/011. Kein Browser, kein Patch.
QUALITÄTSWIRKUNG: PLAUSIBEL weniger Start-Chaos und Chat-Last; Lernen auf Debrief. Unbelegt am Produkt.
KOSTENWIRKUNG: keine Provider. Nur spätere UI-Stunden, in drei GOs.
RISIKEN_UND_OFFENE_HYPOTHESEN:
- H1 Transkript-Schalter vs Repeat.
- H2 Wortzahl-Balken als Uhr gelesen.
- H3 UI-C-Diff bleibt nicht JSX-only — K04 vor Merge.
- Kein Call-Recording im DTO — keine Waveform versprechen.
- Fork U / GO-2-Schluss weiter UNBEKANNT; UI-C zuletzt.
GEGENPRÜFER_URTEIL: FREIGEGEBEN
NÄCHSTE_ROLLE: A00 schneidet UI-A (P1+P6) oder UI-B (P3+P4+P5). A03 nur für Spalten-/Hero-Wortlaut. Nicht A06. UI-C nicht ohne eigenen PR und Freeze-Checkliste.
KONTEXT_FÜR_NÄCHSTEN_CHAT:
- Freeze: liveCall.ts, bargeIn.ts, live-text.ts, openai-realtime.ts, ConversationView-Hooks.
- P1 app/page.tsx: ein StartSessionButton; Rest → /szenario; Cold-Badge bleibt.
- P2/P2b: CallChrome.tsx props-only; ConversationView nur return-JSX; ein PR; Transkript-Default aus bei liveOnly.
- P3 lib/debrief-moments.ts: weight=Wörter, marker aus evaluation, kein ms.
- P4 DebriefTimeline.tsx: Sprung, kein audio, kein TTS v1.
- P5 EvaluationView: Note neben Timeline; Rubrik details.
- P6 lib/session-board.ts + SessionBoard: Offen / Nachbereiten / Ausgewertet; kein Forecast.
- Tests später: debrief-moments + session-board; live-status nicht brechen.
```
