# CHG-20260919-013 — A04 Katalogkarte + Training-Seite (K04)

```yaml
change_id: CHG-20260919-013
status: FREIGEGEBEN
primary_owner: A04
reviewer_k: K04
code: yes
gate: Gate A / G2
```

**Korrektur A00/BO:** Careertrainer-Wissen ja, Careertrainer-Oberfläche nein. Keine drei Tabs, keine Qualify-Konsole, kein Paywall.

## A — Fläche

`/`: Frozen-Stapel, Default = Titel + Badge Telefonat + eine Challenge-Zeile + Initialen/Rollen-Chip + „Gespräch starten“. Optional „Was zählt“ = drei kurze Titel, wieder zu. Keine Fokus-Chips, kein Foto, kein Upgrade.

`/szenario/[id]`: eine Seite — Ziel (ein Satz), `public_brief`, dieselben drei Titel, Hinweiszeile, Start. Kein Tab-Chrome.

Lange Beats/Auftrag/Eval bleiben in `lib/scene-briefing.ts` für A08, `trainingPageSurface` / `catalogSurface` halten sie von der UI fern. Session-POST: nur `scenarioId` + `focusId`.

## K04

```text
GEGENPRÜFUNG
Prüfumfang: / Kartenstapel, /szenario/S01, Isolation POST, Freeze Transport, XSS-Textknoten, Paywall, Silhouette, zwei Kartenzustände.
Durchgeführte Angriffe:
- Browser 127.0.0.1:43147 / und /szenario/S01 (Snapshot + Klick „Was zählt“).
- Unit scene-card / scene-briefing / featured-drill / scenario-product 14/14.
- Grep: scene-briefing nicht in persona-prompt/characters; liveCall/bargeIn unangetastet.
- Session-Body-Test: nur scenarioId+focusId.

BEFUNDE
- K04-1 Hydration-Overlay „1 issue“ an Button auf erstem / -Durchlauf. NOTE. S01 ohne Overlay. Nicht start-blockierend.
- K04-2 callLabel unter der Challenge (Bestand/Kaltakquise). NOTE. Kein Essay, ehrliches Gate-A-Signal.
- K04-3 MicGate „Pegel prüfen“ auf /szenario. NOTE. Kein Tab.
- K04-4 homeStartAction doppelt (featured-drill + scene-card). NOTE. Gleiche Regel.

URTEIL
FREIGEGEBEN
```

```text
AUFTRAG_ID: CHG-20260919-013
ERGEBNIS: Scannbare Katalogkarten + eine Training-Seite. Careertrainer-Konsole nicht geklont.
GEÄNDERTE_DATEIEN_UND_VERTRÄGE: app/page.tsx; app/szenario/[id]/page.tsx; SceneCard.tsx; FeaturedDrillCard.tsx; StartSessionButton (focusPicker default false, Body catalogSessionBody); lib/scene-card.ts; lib/scene-briefing.ts; lib/scenario-product.ts (Beats-Daten); featured-drill homeStartAction; Tests. Session-POST-Vertrag unverändert. Kein Role-Player-/Transport-Vertrag.
NACHWEISE_UND_TESTS: 14 Unit grün. Browser /: drei Frozen-Karten, Telefonat, Challenge, EL/FB, Was zählt → drei Titel. /szenario/S01: Ziel, public_brief, drei Titel, Hinweis, Start. Kein Tab, kein Upgrade.
QUALITÄTSWIRKUNG: PLAUSIBEL weniger Katalog-Last. Kein 2×-Claim.
KOSTENWIRKUNG: keine Provider.
RISIKEN_UND_OFFENE_HYPOTHESEN: Button-Hydration NOTE; A08 verdrahtet qualify/eval noch nicht.
GEGENPRÜFER_URTEIL: FREIGEGEBEN
NÄCHSTE_ROLLE: A00
KONTEXT_FÜR_NÄCHSTEN_CHAT: UI zeigt nur catalogSurface / trainingPageSurface. Lange Felder in toSceneBriefing für A08. Fokus intern defaultFocusForScenario. P0/Transport/Verlauf nicht anfassen.
```
